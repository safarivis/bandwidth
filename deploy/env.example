# Per-customer instance config — copy to /srv/bandwidth/<customer>/env
# The data repo this instance reads (the customer's synced repo):
BOARD_REPO=/srv/bandwidth/<customer>/repo
# Unique localhost port for this instance (nginx routes the subdomain here):
BOARD_PORT=8801
